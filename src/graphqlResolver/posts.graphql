type Post {
  id: ID!
  created: Date!
  author: User!
  name: String!
  body: String!
  comments: [Comment!]!
  commentsCount: Int!
}

type Comment {
  id: ID!
  created: Date!
  author: User!
  body: String!
}

type Query {
  posts(limit: Int, offset: Int): PostPage!
  post(id: ID!): Post!
}

type PostPage {
  totalCount: Int!
  data: [Post!]!
}

type CommentPage {
  totalCount: Int!
  data: [Comment!]!
}
